<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iyxan23.sketch.collab">
    <!-- So we can access the database -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:requestLegacyExternalStorage="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SketchCollab">
        <activity android:name=".SketchwareProjectsListActivity"/>
        <!-- Checks if the server is open -->
        <activity
            android:name=".CheckActivity"
            android:theme="@style/Theme.SketchCollab.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Basically login -->
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.SketchCollab.NoActionBar" /> <!-- The Homepage -->
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.SketchCollab.NoActionBar" />
    </application>

</manifest>